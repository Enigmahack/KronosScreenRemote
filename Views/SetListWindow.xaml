<Window x:Class="KronosScreenRemote.SetListWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set List Viewer"
        Width="900" Height="620"
        MinWidth="560" MinHeight="360"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource TextBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top bar: selector + actions -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 8">
            <TextBlock Text="Set List" VerticalAlignment="Center" Margin="0 0 6 0" Foreground="{StaticResource MutedTextBrush}"/>
            <ComboBox x:Name="CMB_SetList" Width="180"/>
            <Button x:Name="BTN_Load" Content="Load" Width="80" Margin="8 0 0 0"
                    ToolTip="Dump the selected Set List from the Kronos (or show it from cache)."/>
            <Button x:Name="BTN_Refresh" Content="Refresh" Width="80" Margin="6 0 0 0"
                    ToolTip="Re-dump the selected Set List, replacing the cached copy."/>
            <Button x:Name="BTN_EditSlot" Content="Edit Slot…" Width="90" Margin="14 0 0 0"
                    IsEnabled="False"
                    ToolTip="Edit the selected slot's Name and Notes. Performance is read-only — see the hint below."/>
            <TextBlock x:Name="TXT_Status" VerticalAlignment="Center" Margin="14 0 0 0"
                       Foreground="#FF88AADD" TextTrimming="CharacterEllipsis"/>
        </StackPanel>

        <!-- Set List name -->
        <TextBlock Grid.Row="1" x:Name="TXT_Name" Margin="0 0 0 8"
                   FontSize="16" FontWeight="Bold" Foreground="#FFE0E0E0"/>

        <!-- Slot grid -->
        <DataGrid Grid.Row="2" x:Name="DG_Slots"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  Background="#FF141414" Foreground="#FFD0D0D0"
                  RowBackground="#FF141414" AlternatingRowBackground="#FF1B1B1B"
                  BorderBrush="#FF333333" HorizontalGridLinesBrush="#FF2A2A2A"
                  SelectionMode="Single" CanUserAddRows="False" CanUserResizeRows="False"
                  FontFamily="Segoe UI" FontSize="12" RowHeight="24">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Number}" Width="42"/>
                <DataGridTemplateColumn Header="" Width="26">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Width="14" Height="14" CornerRadius="2"
                                    Background="{Binding ColorBrush}" BorderBrush="#FF000000" BorderThickness="1"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Type" Binding="{Binding TypeLabel}" Width="60"/>
                <DataGridTextColumn Header="Performance" Binding="{Binding Performance}" Width="120"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" x:Name="TXT_Hint" Margin="0 8 0 0" FontSize="11" Foreground="#FF777777"
                   TextWrapping="Wrap"
                   Text="Dumps are read from the Kronos once and cached. Requires MIDI monitoring on and SysEx transmit enabled on the Kronos. Empty slots are hidden. Select a slot and click Edit Slot… (or double-click it) to change its Name and Notes — Performance can't be edited from here."/>
    </Grid>
</Window>
