<local:ThemedWindow x:Class="KronosScreenRemote.UnresolvedDependenciesDialog"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:KronosScreenRemote"
        Title="Unresolved Dependencies"
        SizeToContent="Height" Width="480"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TXT_Heading" TextWrapping="Wrap" Margin="0 0 0 10"/>

        <!-- MaxHeight caps growth instead of the window unconditionally sizing to fit every
             row — the bug this dialog replaces a plain MessageBox to fix: with a large number
             of unresolved dependencies (e.g. a big Set List missing many Combis/Programs), the
             box grew so tall its own buttons scrolled off-screen and couldn't be clicked. Below
             the cap this just shows every row with no visible scrollbar; above it, it scrolls. -->
        <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" MaxHeight="360">
            <ListBox x:Name="LST_Dependencies" FontFamily="Consolas" FontSize="12"
                     Background="{StaticResource PanelBackgroundBrush}" Foreground="{StaticResource TextBrush}"
                     BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     DisplayMemberPath="Description"
                     ContextMenuOpening="OnContextMenuOpening">
                <!-- Right-click has to reach the ROW, not just the list, so the menu can act on
                     the specific missing object under the cursor — the whole point of making this
                     dialog actionable instead of a dead end. -->
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <EventSetter Event="PreviewMouseRightButtonDown" Handler="OnRowRightDown"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ContextMenu>
                    <ContextMenu>
                        <MenuItem x:Name="MI_ScanForRow" Header="Search a PCG for this object…" Click="OnScanForRow"/>
                        <MenuItem x:Name="MI_CopyAll" Header="Copy all details" Click="OnCopyAll"/>
                    </ContextMenu>
                </ListBox.ContextMenu>
            </ListBox>
        </Border>

        <!-- Result of a right-click search / copy, shown in place rather than as another modal on
             top of this one (which is itself already blocking a Sync/Commit). -->
        <TextBlock Grid.Row="2" x:Name="TXT_Status" TextWrapping="Wrap" Margin="0 8 0 0"
                   Foreground="{StaticResource MutedTextBrush}"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BTN_Cancel" Content="Cancel" IsCancel="True" IsDefault="True" Click="OnCancel" Margin="0 0 8 0" Style="{StaticResource DialogButtonStyle}"/>
            <Button x:Name="BTN_Continue" Content="Continue Anyway" Click="OnContinue" MinWidth="120"/>
        </StackPanel>
    </Grid>
</local:ThemedWindow>
