<local:ThemedWindow x:Class="KronosScreenRemote.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:KronosScreenRemote"
        Title="Settings" Width="520" Height="700" MinWidth="560" MinHeight="480"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Window.Resources>
        <!-- Key-binding list: app-wide ListViewItem look plus an amber highlight while recording a new key. -->
        <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsListening}" Value="True">
                    <Setter Property="Background" Value="#FF4D3000"/>
                    <Setter Property="BorderBrush" Value="#FFDD9933"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Every slider in this window: double-click resets it to its AppSettings default
             (see OnSliderPreviewMouseLeftButtonDown). Thumb eats MouseDoubleClick, so this
             hooks the Preview tunneling event instead, gated on ClickCount. -->
        <Style TargetType="Slider" BasedOn="{StaticResource {x:Type Slider}}">
            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnSliderPreviewMouseLeftButtonDown"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0" x:Name="SettingsTabControl">

            <!-- ── General ───────────────────────────────────────────────── -->
            <TabItem Header="General">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0 4 0 0">
                    <TextBlock Text="General" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 0 0 8"/>
                    <CheckBox x:Name="ChkPromptQuit"   Content="Prompt before quitting"    Margin="0 0 0 10"
                              ToolTip="Shows a confirmation dialog before the app closes."/>
                    <CheckBox x:Name="ChkHideDataInput"  Content="Hide data input panel on startup"  Margin="0 0 0 10"
                              ToolTip="Hides the right-hand button panel at launch. Toggle from View → Hide Data Input."/>
                    <CheckBox x:Name="ChkHideValueInput" Content="Hide value input panel on startup" Margin="0 0 0 10"
                              ToolTip="Hides the left-hand slider panel at launch. Toggle from View → Hide Value Input."/>
                    <CheckBox x:Name="ChkReverseScrolling" Content="Reverse mouse scrolling direction" Margin="0 0 0 18"
                              ToolTip="Reverses the direction of mouse scrolling."/>

                    <TextBlock Text="Screenshots" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 0 0 8"/>
                    <Grid Margin="0 0 0 4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Output folder:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 0 8 0"
                                   ToolTip="Folder where quick-save screenshots are stored."/>
                        <TextBox   Grid.Column="1" x:Name="TxtScreenshotDir" Margin="0 0 6 0"
                                   ToolTip="Type a path directly, or click Browse to pick one. Leave empty to save next to the app executable."/>
                        <Button    Grid.Column="2" x:Name="BtnBrowseScreenshotDir" Content="Browse..."
                                   Padding="8 3" Click="OnBrowseScreenshotDir"
                                   ToolTip="Open a folder picker to choose the screenshot output folder."/>
                    </Grid>
                    <TextBlock Margin="0 0 0 18" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="Folder for quick-save screenshots. Leave empty to save next to the app executable."/>

                    <TextBlock Text="VGA output" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 0 0 8"/>
                    <CheckBox x:Name="ChkVgaMirror" Content="Enable VGA mirror on connect" Margin="0 0 0 12"
                              ToolTip="Mirrors the Kronos touchscreen to the VGA output when connected. Applied immediately on OK."/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Screensaver timeout" VerticalAlignment="Center"
                                   ToolTip="Seconds of inactivity before the Kronos screensaver activates. 0 disables it. Applied immediately on OK."/>
                        <TextBox   Grid.Column="1" x:Name="TxtSsTimeout" Width="55" HorizontalAlignment="Left"
                                   ToolTip="Seconds of inactivity before the Kronos screensaver activates. 0 disables it."/>
                        <TextBlock Grid.Column="2" Text="seconds  (0 = disabled)" VerticalAlignment="Center"
                                   Margin="8 0 0 0" Foreground="#FF888888" FontSize="11"/>
                    </Grid>
                    <TextBlock Margin="0 6 0 0" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="VGA mirror and screensaver settings are applied immediately when you click OK while connected."/>

                    <Border Background="#FF444444" Margin="0 18 0 12" Height="1"/>
                    <TextBlock Text="Reset" FontWeight="Bold" Foreground="#FFCC4444" Margin="0 0 0 8"/>
                    <Button x:Name="BtnResetSettings"
                            Content="Reset All Settings"
                            Width="150"
                            HorizontalAlignment="Left"
                            Background="#FF3A1E1E"
                            Foreground="#FFDD6666"
                            BorderBrush="#FF883333"
                            Click="OnResetSettings"
                            ToolTip="Permanently deletes all settings, key maps, calibration, and customizations. Cannot be undone."/>
                    <TextBlock Margin="0 8 0 0" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="Removes all saved settings, key mappings, calibration data, and other customizations. The app returns to its default state. This cannot be undone."/>
                </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ── Connection ───────────────────────────────────────────── -->
            <TabItem Header="Connection">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <Grid Margin="0 4 0 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Kronos IP address" VerticalAlignment="Center"
                                   ToolTip="IP address of the Kronos. Typically 192.168.100.15 over USB, or a DHCP address when connected via a router."/>
                        <TextBox   Grid.Row="0" Grid.Column="1" x:Name="TxtHost"
                                   ToolTip="IP address of the Kronos. Typically 192.168.100.15 over USB, or a DHCP address when connected via a router."/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Stream port" VerticalAlignment="Center"
                                   ToolTip="TCP port the screen-streaming daemon listens on. Default: 7373. Change only if remapped on the Kronos."/>
                        <TextBox   Grid.Row="2" Grid.Column="1" x:Name="TxtStreamPort" Width="80" HorizontalAlignment="Left"
                                   ToolTip="TCP port the screen-streaming daemon listens on. Default: 7373."/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Control port" VerticalAlignment="Center"
                                   ToolTip="TCP port the control daemon listens on (button presses, mode queries). Default: 7374. Change only if remapped on the Kronos."/>
                        <TextBox   Grid.Row="4" Grid.Column="1" x:Name="TxtCtrlPort"  Width="80" HorizontalAlignment="Left"
                                   ToolTip="TCP port the control daemon listens on. Default: 7374."/>
                    </Grid>
                    <TextBlock Margin="0 12 0 0" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="Connection changes take effect on the next connect. The --host, --port, and --ctrl command-line arguments override these values for the current session only."/>

                    <Border Background="#FF444444" Margin="0 16 0 12" Height="1"/>
                    <TextBlock Text="FTP Credentials" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 0 0 8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="FTP Username" VerticalAlignment="Center"
                                   ToolTip="Username for FTP authentication to the Kronos."/>
                        <TextBox   Grid.Row="0" Grid.Column="1" x:Name="TxtFtpUser"
                                   ToolTip="Username for FTP authentication to the Kronos."/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="FTP Password" VerticalAlignment="Center"
                                   ToolTip="Password for FTP authentication to the Kronos."/>
                        <PasswordBox Grid.Row="2" Grid.Column="1" x:Name="TxtFtpPass"
                                     ToolTip="Password for FTP authentication to the Kronos."/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="FTP Port" VerticalAlignment="Center"
                                   ToolTip="TCP port for FTP connections to the Kronos. Default: 21."/>
                        <TextBox   Grid.Row="4" Grid.Column="1" x:Name="TxtFtpPort" Width="80" HorizontalAlignment="Left"
                                   ToolTip="TCP port for FTP connections to the Kronos. Default: 21."/>
                    </Grid>
                    <Button x:Name="BtnClearFtp" Content="Clear FTP Credentials"
                            Width="170" HorizontalAlignment="Left" Margin="0 12 0 0"
                            Click="OnClearFtpCredentials"
                            ToolTip="Clear the saved FTP username and password."/>
                    <TextBlock Margin="0 12 0 0" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="FTP credentials are used for the File Manager and for stream authentication. If left empty, you will be prompted when connecting."/>
                </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ── Streaming ─────────────────────────────────────────────── -->
            <TabItem Header="Streaming">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <TextBlock Text="Stream mode" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 4 0 8"/>
                    <ComboBox x:Name="CMB_StreamMode" HorizontalAlignment="Left" MinWidth="200"
                              BorderBrush="#FF555555"
                              ToolTip="How frames are delivered from the daemon. Takes effect on the next connect.">
                        <ComboBoxItem Content="Change-driven - send frames only when the screen changes (recommended)"
                                      ToolTip="Recommended. The daemon sends a frame only when the display changes - uses almost no CPU when the Kronos is idle."/>
                        <ComboBoxItem Content="Pull - client requests each frame individually"
                                      ToolTip="The client requests each frame individually. Try this if frames are occasionally missed in change-driven mode."/>
                    </ComboBox>

                    <TextBlock Text="Max frame rate" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 18 0 8"/>
                    <DockPanel LastChildFill="True">
                        <TextBlock x:Name="TxtFpsLabel" DockPanel.Dock="Right" Width="50"
                                   VerticalAlignment="Center" TextAlignment="Right"/>
                        <Slider x:Name="SlFps" Minimum="1" Maximum="15"
                                TickFrequency="1" IsSnapToTickEnabled="True"
                                ValueChanged="SlFps_ValueChanged"
                                ToolTip="Maximum frames per second the daemon will deliver. 15 fps matches the Kronos display refresh rate."/>
                    </DockPanel>
                    <TextBlock Margin="0 6 0 0" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="Change-driven mode consumes no CPU when the Kronos screen is idle. Pull mode may help if frames are occasionally missed. Streaming changes take effect on the next connect."/>

                </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ── View ─────────────────────────────────────────────────── -->
            <TabItem Header="View">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0 4 0 0">
                    <TextBlock Text="Window" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 0 0 8"/>
                    <Grid Margin="0 0 0 6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Default window size" VerticalAlignment="Center"
                                   ToolTip="Window size applied at launch."/>
                        <ComboBox Grid.Column="1" x:Name="CMB_DefaultWindowSize" MinWidth="150"
                                  BorderBrush="#FF555555"
                                  ToolTip="Last Used keeps remembering the size/position you leave the window at.">
                            <ComboBoxItem Content="Last Used"/>
                            <ComboBoxItem Content="Small"/>
                            <ComboBoxItem Content="Medium"/>
                            <ComboBoxItem Content="Large"/>
                            <ComboBoxItem Content="Maximized"/>
                        </ComboBox>
                    </Grid>
                    <TextBlock Margin="0 6 0 0" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="Size applied to the main window at launch. Last Used remembers the size and position you leave it at."/>

                    <Border Background="#FF444444" Margin="0 16 0 12" Height="1"/>
                    <TextBlock Text="Zoom" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 0 0 8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Default zoom level (×)" VerticalAlignment="Center"
                                   ToolTip="Zoom multiplier used when zoom is first activated and when reset. Also the minimum - zooming out stops here."/>
                        <Slider    Grid.Column="1" x:Name="SlZoomLevel"
                                   Minimum="2.5" Maximum="5.0"
                                   TickFrequency="0.5" IsSnapToTickEnabled="True"
                                   ValueChanged="SlZoomLevel_ValueChanged"
                                   ToolTip="Zoom multiplier used when zoom is first activated and when reset. Also the minimum - zooming out stops here."/>
                        <TextBlock Grid.Column="2" x:Name="TxtZoomLabel"
                                   VerticalAlignment="Center" Margin="6 0 0 0" TextAlignment="Right"/>
                    </Grid>
                    <TextBlock Margin="0 8 0 0" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="The zoom level used when zoom is first activated and when it is reset. This is also the minimum - zooming out stops here. Zoom In/Out key bindings are set in the Key Bindings tab."/>

                    <Border Background="#FF444444" Margin="0 16 0 12" Height="1"/>
                    <TextBlock Text="Zoom Tool" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 0 0 8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Tool size" VerticalAlignment="Center"
                                   ToolTip="Size of the zoom loupe overlay. 1× = 200×150 px. Snaps to 0.5× steps; slide freely between snap points."/>
                        <Slider    Grid.Column="1" x:Name="SlZoomWindowSize"
                                   Minimum="1.0" Maximum="3.5"
                                   SmallChange="0.1" LargeChange="0.5"
                                   ValueChanged="SlZoomWindowSize_ValueChanged"
                                   ToolTip="Size of the zoom loupe overlay. 1× = 200×150 px. Snaps to 0.5× steps; slide freely between snap points."/>
                        <TextBlock Grid.Column="2" x:Name="TxtZoomWindowSizeLabel"
                                   VerticalAlignment="Center" Margin="6 0 0 0" TextAlignment="Right"/>
                    </Grid>
                    <TextBlock Margin="0 6 0 0" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="Size of the zoom window overlay at 1× (200×150 px). Snaps to 1×–3.5× at 0.5× intervals; slide freely between snap points."/>
                </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ── Image ─────────────────────────────────────────────────── -->
            <TabItem Header="Image">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0 4 0 0">
                    <TextBlock Text="Upscaling filter" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 0 0 8"/>
                    <ComboBox x:Name="CMB_ScalingMode" HorizontalAlignment="Left" MinWidth="200"
                              BorderBrush="#FF555555"
                              ToolTip="How the 800×600 Kronos screen is resampled when enlarged to fill the window.">
                        <ComboBoxItem Content="Sharp - crisp, blocky pixels (nearest-neighbor)"
                                      ToolTip="No interpolation. Razor-sharp edges, but visible pixel blocks when enlarged."/>
                        <ComboBoxItem Content="Smooth - soft, fast (bilinear)"
                                      ToolTip="Bilinear filtering. Soft and inexpensive."/>
                        <ComboBoxItem Content="High Quality - best resample (Fant)"
                                      ToolTip="Fant resampling. Best quality; the default."/>
                    </ComboBox>
                    <TextBlock Margin="0 8 0 0" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="Controls how the 800×600 Kronos screen is scaled up to fill the window. Also available from View → Scaling Quality and the frame right-click menu."/>

                    <Border Background="#FF444444" Margin="0 16 0 12" Height="1"/>
                    <TextBlock Text="Adjustments" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 0 0 8"/>

                    <!-- Brightness -->
                    <Grid Margin="0 0 0 8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Brightness" VerticalAlignment="Center"
                                   ToolTip="Lighten or darken the whole image. 0 = no change."/>
                        <Slider    Grid.Column="1" x:Name="SlBrightness" Minimum="-100" Maximum="100"
                                   TickFrequency="1" IsSnapToTickEnabled="True"
                                   ValueChanged="SlBrightness_ValueChanged"
                                   ToolTip="Lighten or darken the whole image. 0 = no change."/>
                        <TextBlock Grid.Column="2" x:Name="TxtBrightnessLabel"
                                   VerticalAlignment="Center" Margin="6 0 0 0" TextAlignment="Right"/>
                    </Grid>

                    <!-- Contrast -->
                    <Grid Margin="0 0 0 8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Contrast" VerticalAlignment="Center"
                                   ToolTip="Spread or compress tones around mid-grey. 0 = no change."/>
                        <Slider    Grid.Column="1" x:Name="SlContrast" Minimum="-100" Maximum="100"
                                   TickFrequency="1" IsSnapToTickEnabled="True"
                                   ValueChanged="SlContrast_ValueChanged"
                                   ToolTip="Spread or compress tones around mid-grey. 0 = no change."/>
                        <TextBlock Grid.Column="2" x:Name="TxtContrastLabel"
                                   VerticalAlignment="Center" Margin="6 0 0 0" TextAlignment="Right"/>
                    </Grid>

                    <!-- Gamma -->
                    <Grid Margin="0 0 0 8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Gamma" VerticalAlignment="Center"
                                   ToolTip="Non-linear midtone correction. 1.00 = no change; higher brightens midtones."/>
                        <Slider    Grid.Column="1" x:Name="SlGamma" Minimum="0.4" Maximum="2.5"
                                   SmallChange="0.05" LargeChange="0.1"
                                   ValueChanged="SlGamma_ValueChanged"
                                   ToolTip="Non-linear midtone correction. 1.00 = no change; higher brightens midtones, useful for the dark Kronos UI."/>
                        <TextBlock Grid.Column="2" x:Name="TxtGammaLabel"
                                   VerticalAlignment="Center" Margin="6 0 0 0" TextAlignment="Right"/>
                    </Grid>

                    <!-- Saturation -->
                    <Grid Margin="0 0 0 8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Saturation" VerticalAlignment="Center"
                                   ToolTip="Color vividness. 0 = no change; -100 = greyscale."/>
                        <Slider    Grid.Column="1" x:Name="SlSaturation" Minimum="-100" Maximum="100"
                                   TickFrequency="1" IsSnapToTickEnabled="True"
                                   ValueChanged="SlSaturation_ValueChanged"
                                   ToolTip="Color vividness. 0 = no change; -100 = greyscale; +100 = double."/>
                        <TextBlock Grid.Column="2" x:Name="TxtSaturationLabel"
                                   VerticalAlignment="Center" Margin="6 0 0 0" TextAlignment="Right"/>
                    </Grid>

                    <!-- Sharpen -->
                    <Grid Margin="0 0 0 4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Sharpen" VerticalAlignment="Center"
                                   ToolTip="Unsharp-mask edge enhancement. 0 = off. Recovers crispness lost to smooth/HQ scaling."/>
                        <Slider    Grid.Column="1" x:Name="SlSharpen" Minimum="0" Maximum="100"
                                   TickFrequency="1" IsSnapToTickEnabled="True"
                                   ValueChanged="SlSharpen_ValueChanged"
                                   ToolTip="Unsharp-mask edge enhancement. 0 = off. Adds a light per-frame sharpening pass; recovers crispness lost to smooth/HQ upscaling."/>
                        <TextBlock Grid.Column="2" x:Name="TxtSharpenLabel"
                                   VerticalAlignment="Center" Margin="6 0 0 0" TextAlignment="Right"/>
                    </Grid>

                    <Button x:Name="BtnResetImage" Content="Reset Adjustments"
                            Width="150" HorizontalAlignment="Left" Margin="0 10 0 0"
                            Click="OnResetImageAdjust"
                            ToolTip="Reset brightness, contrast, gamma, saturation, and sharpen to their defaults."/>
                    <TextBlock Margin="0 8 0 0" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="Brightness, contrast, gamma, and saturation are applied for free through the palette; sharpen adds a light per-frame pass. All adjustments are baked into screenshots and clipboard copies."/>
                </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ── Key Bindings ──────────────────────────────────────────── -->
            <TabItem Header="Key Bindings">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ListView x:Name="KeyList" Grid.Row="0"
                              SelectionMode="Single"
                              MouseDoubleClick="OnKeyListDoubleClick"
                              KeyDown="OnKeyListKeyDown"
                              ToolTip="Double-click a row to rebind it. Hold Ctrl/Alt/Shift before pressing the key to set a chord. Press Delete to clear the selected binding. Escape cancels.">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Action" Width="250" DisplayMemberBinding="{Binding Label}"/>
                                <GridViewColumn Header="Key"    Width="160" DisplayMemberBinding="{Binding DisplayKey}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <TextBlock Grid.Row="2" Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="Double-click a row to rebind it. Hold Ctrl/Alt/Shift before pressing the key to set a chord. Press Delete to clear the selected binding. Escape cancels."/>
                </Grid>
            </TabItem>

            <!-- ── Input Mapping (host key → Kronos raw keycode) ───────────── -->
            <TabItem Header="Input Mapping">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" MinHeight="60"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0 4 0 0">
                        <TextBlock DockPanel.Dock="Top"
                                   Text="Keyboard Input Mapping"
                                   FontWeight="Bold" Foreground="#FF88AADD"
                                   Margin="0 0 0 8"/>
                        <DockPanel DockPanel.Dock="Top" LastChildFill="False" Margin="0 0 0 5">
                            <Button x:Name="BtnRawAdd"        Content="Add"         Margin="0 0 4 0"
                                    DockPanel.Dock="Left"  Click="OnRawAdd"
                                    ToolTip="Add a custom mapping from a host key to a Kronos raw keycode."/>
                            <Button x:Name="BtnRawRemove"     Content="Remove"      IsEnabled="False"
                                    DockPanel.Dock="Left"  Click="OnRawRemove"
                                    ToolTip="Remove the selected raw key mapping."/>
                            <Button x:Name="BtnInputTester"   Content="Test Inputs" IsEnabled="False" Visibility="Hidden"
                                    DockPanel.Dock="Right" Click="OnInputTesterClick"/>
                        </DockPanel>
                        <TextBlock DockPanel.Dock="Bottom" Margin="0 6 0 0"
                                   Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                                   Text="Custom mappings override the default key map and take effect immediately. Select a row to edit it below."/>
                        <ListView x:Name="RawList" SelectionMode="Single"
                                  SelectionChanged="OnRawSelectionChanged"
                                  ToolTip="Custom host-key → Kronos keycode mappings. These override the default key map and apply immediately. Select a row to edit it below.">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Host Key" Width="100" DisplayMemberBinding="{Binding HostKeyDisplay}"/>
                                    <GridViewColumn Header="→ Sends"  Width="110" DisplayMemberBinding="{Binding RawDisplay}"/>
                                    <GridViewColumn Header="Label"    Width="210" DisplayMemberBinding="{Binding Label}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>

                    <!-- Inline edit panel - always present; faded/disabled until a row is
                         selected or Add is clicked (see SetRawEditorLit), never collapsed. -->
                    <Grid Grid.Row="1" x:Name="RawEditor" Opacity="0.35" IsEnabled="False">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="6"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="6"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#FF444444"/>

                        <!-- Label -->
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="76"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Label:" VerticalAlignment="Center"/>
                            <TextBox   Grid.Column="1" x:Name="TxtRawLabel"/>
                        </Grid>

                        <!-- Host key capture -->
                        <Grid Grid.Row="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="76"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Host key:" VerticalAlignment="Center"/>
                            <Button    Grid.Column="1" x:Name="BtnRawCaptureKey" Content="(none)"
                                       HorizontalContentAlignment="Left"
                                       Click="OnRawCaptureKey"/>
                        </Grid>

                        <!-- Raw code + shift -->
                        <Grid Grid.Row="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="76"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Raw code:" VerticalAlignment="Center"/>
                            <TextBox   Grid.Column="1" x:Name="TxtRawCode" Width="55" HorizontalAlignment="Left"/>
                            <CheckBox  Grid.Column="2" x:Name="ChkRawShift" Content="Send Shift"
                                       Margin="12 0 0 0" VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Save / Cancel - bottom right of this pane, matching the window's own Cancel/OK order -->
                        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="BtnRawCancelEdit" Content="Cancel" Width="60" Margin="0 0 6 0"
                                    Click="OnRawCancelEdit"/>
                            <Button x:Name="BtnRawSave"       Content="Save"   Width="60"
                                    Click="OnRawSave"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- ── Macros ────────────────────────────────────────────────── -->
            <TabItem Header="Macros">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" MinHeight="80"/>
                        <RowDefinition Height="1"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- List + toolbar -->
                    <DockPanel Grid.Row="0" Margin="0 4 0 0">
                        <DockPanel DockPanel.Dock="Top" Margin="0 0 0 5" LastChildFill="False">
                            <Button x:Name="BtnMacroAdd"    Content="Add"    Margin="0 0 4 0"
                                    DockPanel.Dock="Left"  Click="OnMacroAdd"
                                    ToolTip="Create a new empty macro."/>
                            <Button x:Name="BtnMacroRemove" Content="Remove" IsEnabled="False"
                                    DockPanel.Dock="Left"  Click="OnMacroRemove"
                                    ToolTip="Delete the selected macro."/>
                            <Button x:Name="BtnMacroPlay"   Content="▶ Play" IsEnabled="False"
                                    DockPanel.Dock="Right" Click="OnMacroPlay"
                                    ToolTip="Play the selected macro once immediately."/>
                        </DockPanel>
                        <ListView x:Name="MacroList" SelectionMode="Single"
                                  SelectionChanged="OnMacroSelectionChanged"
                                  ToolTip="Select a macro to edit its steps, trigger key, and delay below.">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Description" Width="170" DisplayMemberBinding="{Binding Description}"/>
                                    <GridViewColumn Header="Trigger"     Width="110" DisplayMemberBinding="{Binding TriggerDisplay}"/>
                                    <GridViewColumn Header="Steps"       Width="44"  DisplayMemberBinding="{Binding StepCount}"/>
                                    <GridViewColumn Header="Delay"       Width="54"  DisplayMemberBinding="{Binding DelayDisplay}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>

                    <!-- Divider -->
                    <Border Grid.Row="1" Background="#FF444444" Margin="0 6 0 4"/>

                    <!-- Editor panel (disabled until a row is selected) -->
                    <Grid Grid.Row="2" x:Name="MacroEditor" IsEnabled="False">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="6"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="6"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="6"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="6"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Row 0: Description -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="78"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Description:" VerticalAlignment="Center"
                                       ToolTip="Name shown in the macro list."/>
                            <TextBox   Grid.Column="1" x:Name="TxtMacroDesc" TextChanged="OnMacroDescChanged"
                                       ToolTip="Name shown in the macro list."/>
                        </Grid>

                        <!-- Row 2: Trigger -->
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="78"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Trigger:" VerticalAlignment="Center"
                                       ToolTip="Keyboard shortcut that fires this macro automatically. Must include Ctrl, Alt, or Shift."/>
                            <Button    Grid.Column="1" x:Name="BtnMacroTrigger" Content="(none)"
                                       HorizontalContentAlignment="Left"
                                       Click="OnMacroTriggerClick"
                                       ToolTip="Click, then press a key combination to assign a trigger. Must include Ctrl, Alt, or Shift. Press Escape to cancel."/>
                        </Grid>

                        <!-- Row 4: Step delay slider -->
                        <Grid Grid.Row="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="78"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="48"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Step delay:" VerticalAlignment="Center"
                                       ToolTip="Pause between each key event during macro playback."/>
                            <Slider    Grid.Column="1" x:Name="SlMacroDelay"
                                       Minimum="10" Maximum="500" TickFrequency="10" IsSnapToTickEnabled="True"
                                       ValueChanged="OnMacroDelayChanged"
                                       ToolTip="Pause between each key event during macro playback. Increase if the Kronos misses steps."/>
                            <TextBlock Grid.Column="2" x:Name="TxtMacroDelayLabel"
                                       VerticalAlignment="Center" Margin="6 0 0 0" TextAlignment="Right"/>
                        </Grid>

                        <!-- Row 6: Steps readout -->
                        <TextBox Grid.Row="6" x:Name="TxtMacroSteps"
                                 IsReadOnly="True" TextWrapping="Wrap" Height="44"
                                 Background="#FF1E1E1E" Foreground="#FFBBBBBB"
                                 VerticalScrollBarVisibility="Auto" Padding="3 2"
                                 Text="(no steps recorded)"
                                 ToolTip="The recorded key presses and releases that make up this macro. Read-only - use Record to capture new steps."/>

                        <!-- Row 8: Record / Clear + hint -->
                        <DockPanel Grid.Row="8">
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                <Button x:Name="BtnMacroRecord" Content="● Record" Width="84" Margin="0 0 6 0"
                                        Click="OnMacroRecordToggle"
                                        ToolTip="Start recording keystrokes as macro steps. Click again (■ Stop) to finish. Replaces any existing steps."/>
                                <Button x:Name="BtnMacroClear"  Content="Clear"    Width="60"
                                        Click="OnMacroClear"
                                        ToolTip="Remove all recorded steps from this macro."/>
                            </StackPanel>
                            <TextBlock VerticalAlignment="Center" Margin="10 0 0 0"
                                       Foreground="#FF666666" FontSize="11" TextWrapping="Wrap"
                                       Text="Best with navigation and control keys. Trigger must include a modifier (Ctrl/Alt/Shift)."/>
                        </DockPanel>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- ── Debug ─────────────────────────────────────────────────── -->
            <TabItem Header="Debug">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0 4 0 0">
                    <!-- Logging -->
                    <CheckBox x:Name="ChkDebugLogging" Content="Enable debug logging"
                              Margin="0 0 0 10"
                              ToolTip="Writes verbose DEBUG entries to the log file. Takes effect immediately. Enable only when diagnosing issues - it can grow the log quickly."/>
                    <TextBlock Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="When enabled, verbose DEBUG entries are written to the log file. Only Info, Warn, and Error are logged when this is off. Takes effect immediately."/>
                </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ── MIDI / SysEx ──────────────────────────────────────────────── -->
            <TabItem Header="MIDI/SysEx">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0 4 0 0">
                    <Grid Margin="0 0 0 6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="MIDI transport" VerticalAlignment="Center"
                                   Foreground="#FF888888" FontSize="11"/>
                        <ComboBox Grid.Column="1" x:Name="CMB_MidiTransport"
                                  BorderBrush="#FF555555" Width="160"
                                  ToolTip="Which path carries MIDI/SysEx to the Kronos. The screen/video is always over TCP. Auto prefers a directly-connected Kronos USB device.">
                            <ComboBoxItem Content="Auto (prefer USB)"/>
                            <ComboBoxItem Content="TCP (daemon)"/>
                            <ComboBoxItem Content="USB (direct)"/>
                        </ComboBox>
                    </Grid>
                    <TextBlock x:Name="TXT_UsbDeviceStatus" Foreground="#FF888888" FontSize="11"
                               TextWrapping="Wrap" Margin="0 0 0 16"
                               Text="Detecting USB MIDI devices..."/>

                    <Border Background="#FF444444" Margin="0 0 0 14" Height="1"/>

                    <CheckBox x:Name="ChkMidiMonitor" Content="Monitor MIDI" Margin="0 0 0 6"
                              ToolTip="When disabled, all incoming MIDI and SysEx messages are ignored and the MIDI Monitor window is unavailable."/>
                    <TextBlock Foreground="#FF888888" FontSize="11" TextWrapping="Wrap" Margin="0 0 0 16"
                               Text="When disabled, the MIDI/SysEx Monitor option is faded in the menu and no incoming MIDI data is processed."/>

                    <Border Background="#FF444444" Margin="0 0 0 14" Height="1"/>

                    <CheckBox x:Name="ChkSysExPollOnChanges" Content="SysEx Poll on Changes" Margin="0 0 0 6"
                              ToolTip="Triggers a SysEx check-in when the Kronos sends a Program Change or Bank Select message. Requires Monitor MIDI to be enabled."/>
                    <TextBlock Foreground="#FF888888" FontSize="11" TextWrapping="Wrap" Margin="0 0 0 16"
                               Text="Automatically updates the current performance display when a Program Change or Bank Select is received from the Kronos."/>

                    <Border Background="#FF444444" Margin="0 0 0 14" Height="1"/>

                    <CheckBox x:Name="ChkPullNamesOnChange" Content="Pull Names on Program Change" Margin="0 0 0 6"
                              ToolTip="When you select a program/combi whose name isn't cached yet, fetch just that name over MIDI. Best over USB, where a name fetch takes milliseconds. Requires Monitor MIDI."/>
                    <TextBlock Foreground="#FF888888" FontSize="11" TextWrapping="Wrap" Margin="0 0 0 16"
                               Text="Fills the performance name as you navigate, without a full Sync Names sweep. Over the daemon/DIN path this can be slow and briefly flash the Kronos display; over USB it is near-instant."/>

                    <Border Background="#FF444444" Margin="0 0 0 14" Height="1"/>

                    <CheckBox x:Name="ChkProactivePoll" Content="Proactive SysEx Polling" Margin="0 0 0 6"
                              ToolTip="Enabling this can cause the Kronos to slow down during the check-in. Only enable if you require it."
                              Checked="OnProactivePollChanged" Unchecked="OnProactivePollChanged"/>
                    <TextBlock Foreground="#FF888888" FontSize="11" TextWrapping="Wrap" Margin="0 0 0 12"
                               Text="Periodically queries the Kronos for the current performance name on a fixed schedule, regardless of MIDI activity."/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Poll interval" VerticalAlignment="Center"
                                   Foreground="#FF888888" FontSize="11"/>
                        <ComboBox Grid.Column="1" x:Name="CMB_PollInterval"
                                  BorderBrush="#FF555555"
                                  Width="120" IsEnabled="False">
                            <ComboBoxItem Content="30 seconds"/>
                            <ComboBoxItem Content="45 seconds"/>
                            <ComboBoxItem Content="60 seconds"/>
                            <ComboBoxItem Content="120 seconds"/>
                        </ComboBox>
                    </Grid>

                    <Border Background="#FF444444" Margin="0 14 0 14" Height="1"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Value slider CC#" VerticalAlignment="Center"
                                   Foreground="#FF888888" FontSize="11"/>
                        <TextBox Grid.Column="1" x:Name="TXT_ValueSliderCc"
                                 BorderBrush="#FF555555" Width="60" MaxLength="3"
                                 ToolTip="MIDI CC# the Kronos VALUE slider transmits (default 18). The UI value slider follows this controller. Change it if your Kronos assigns the value slider to a different CC."/>
                    </Grid>
                    <TextBlock Foreground="#FF888888" FontSize="11" TextWrapping="Wrap" Margin="0 6 0 0"
                               Text="Keeps the on-screen value slider in sync with physical VALUE slider moves on the Kronos. Requires Monitor MIDI and SysEx transmit enabled on the Kronos."/>
                </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Librarian">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0 4 0 0">
                    <Grid Margin="0 0 0 6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Merge behavior" VerticalAlignment="Center"
                                   Foreground="#FF888888" FontSize="11"/>
                        <ComboBox Grid.Column="1" x:Name="CMB_MergeBehavior" BorderBrush="#FF555555" Width="200">
                            <ComboBoxItem Content="Temporary Memory"
                                          ToolTip="Will store in memory only. Restarting the app clears the merge data."/>
                            <ComboBoxItem Content="Local Storage"
                                          ToolTip="Stores on local drive - persists reboots but could be slower."/>
                        </ComboBox>
                    </Grid>
                    <TextBlock Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="Controls whether the Librarian's Merge Window (a staging area for objects pulled from loaded PCG files, before they're placed into your Local Library) survives an app restart or crash."/>

                    <Border Background="#FF444444" Margin="0 14 0 14" Height="1"/>

                    <CheckBox x:Name="ChkPreserveDupPrograms" Content="Programs: preserve duplication"
                              Margin="0 0 0 6"
                              ToolTip="Checked: placing a Program from the Merge Window always copies it as-is into a fresh slot, even if byte-identical content already exists in your Local Library. Unchecked (default): duplicates are detected and the existing copy is reused."/>
                    <CheckBox x:Name="ChkPreserveDupCombis" Content="Combis: preserve duplication"
                              Margin="0 0 0 6"
                              ToolTip="Checked (default): placing a Combi from the Merge Window always copies it as-is into a fresh slot. Unchecked: duplicates are detected and the existing copy is reused - compared after its Program references are re-pointed, so a re-copied PCG still matches."/>
                    <TextBlock Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="When placing from the Merge Window into Local Library, choose per object type whether duplicates are preserved (copied as-is) or detected and reused. Also available as quick toggles in the Merge Window toolbar; both stay in sync."/>

                    <Border Background="#FF444444" Margin="0 14 0 14" Height="1"/>

                    <CheckBox x:Name="ChkLibrarianFullSyncOnLaunch" Content="Full sync on launch"
                              Margin="0 0 0 6"
                              ToolTip="Pull every bank from the Kronos as soon as the Librarian opens. Never writes to the Kronos."/>
                    <TextBlock Foreground="#FF888888" FontSize="11" TextWrapping="Wrap" Margin="0 0 0 12"
                               Text="Reads every program, combi, set list, drum kit, and wave sequence instead of only the banks whose digest changed. Slow on a full library and is a pull only: nothing is pushed to the Kronos without user intervention."/>

                    <CheckBox x:Name="ChkLibrarianForceDestructiveWrite" Content="Force destructive write"
                              Margin="0 0 0 6"
                              ToolTip="2-Way Sync overwrites the Kronos even where it changed since the last pull, without asking."/>
                    <TextBlock Foreground="#FFCC8888" FontSize="11" TextWrapping="Wrap"
                               Text="With this on, the local library is treated as the source of truth and 2-Way Sync overwrites the Kronos to match it, without asking."/>
                </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ── Sample Editor ────────────────────────────────────────── -->
            <TabItem Header="Sample Editor">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0 4 0 0">
                    <TextBlock Text="Playback" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 0 0 8"/>
                    <Grid Margin="0 0 0 6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Output device" VerticalAlignment="Center"
                                   Foreground="#FF888888" FontSize="11"/>
                        <ComboBox Grid.Column="1" x:Name="CMB_SampleOutputDevice"
                                  BorderBrush="#FF555555" MinWidth="220"
                                  DisplayMemberPath="Name" SelectedValuePath="Id"
                                  ToolTip="Which audio output the Sample Editor plays samples through."/>
                    </Grid>
                    <TextBlock Foreground="#FF888888" FontSize="11" TextWrapping="Wrap" Margin="0 0 0 16"
                               Text="Takes effect the next time the Sample Editor window is opened."/>

                    <Border Background="#FF444444" Margin="0 0 0 14" Height="1"/>

                    <TextBlock Text="Create Zone Preferences" FontWeight="Bold" Foreground="#FF88AADD" Margin="0 0 0 8"/>

                    <TextBlock Text="Position" Foreground="#FF888888" FontSize="11" Margin="0 0 0 4"/>
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                        <RadioButton x:Name="RadZoneCreateRight" Content="Right" GroupName="ZoneCreatePosition" Margin="0 0 16 0"
                                     ToolTip="A newly created zone is appended above the current top zone's own Top Key, without shrinking or otherwise changing it. Only falls back to shrinking the top zone from above if it's already at the very top of the MIDI range (127)."/>
                        <RadioButton x:Name="RadZoneCreateLeft" Content="Left" GroupName="ZoneCreatePosition"
                                     ToolTip="A newly created zone takes the lower-key portion of the current last zone's range instead; the existing last zone keeps its own Top Key."/>
                    </StackPanel>

                    <Grid Margin="0 0 0 10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Zone Range" VerticalAlignment="Center" Foreground="#FF888888" FontSize="11"/>
                        <TextBox   Grid.Column="1" x:Name="TxtZoneCreateRange" Width="50" HorizontalAlignment="Left"
                                   ToolTip="How many keys (1-127) a newly created zone claims from the current last zone's range."/>
                        <TextBlock Grid.Column="2" Text="keys  (1 to 127)" VerticalAlignment="Center" Margin="8 0 0 0"
                                   Foreground="#FF888888" FontSize="11"/>
                    </Grid>

                    <TextBlock Text="Original Key Position" Foreground="#FF888888" FontSize="11" Margin="0 0 0 4"/>
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                        <RadioButton x:Name="RadOrigKeyBottom" Content="Bottom" GroupName="ZoneOrigKeyPos" Margin="0 0 16 0"/>
                        <RadioButton x:Name="RadOrigKeyCenter" Content="Center" GroupName="ZoneOrigKeyPos" Margin="0 0 16 0"/>
                        <RadioButton x:Name="RadOrigKeyTop"    Content="Top"    GroupName="ZoneOrigKeyPos"/>
                    </StackPanel>
                    <TextBlock Foreground="#FF888888" FontSize="11" TextWrapping="Wrap"
                               Text="Where the Original Key (the root/tracking key - independent of the trigger range itself) lands within a newly created zone's key range."/>
                </StackPanel>
                </ScrollViewer>
            </TabItem>

        </TabControl>

        <!-- Bottom row: Import/Export left, OK/Cancel right -->
        <Grid Grid.Row="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="BtnExport" Content="Export..." Width="80" Margin="0 0 8 0"
                        Click="OnExport"
                        ToolTip="Save all current settings to a JSON file for backup or sharing."/>
                <Button x:Name="BtnImport" Content="Import..." Width="80"
                        Click="OnImport"
                        ToolTip="Load settings from a JSON file, replacing the current configuration."/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel" Content="Cancel" Width="80" Margin="0 0 8 0"
                        Click="BtnCancel_Click" IsCancel="True"/>
                <Button x:Name="BtnOK"     Content="OK"     Width="80"
                        Click="BtnOK_Click" IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</local:ThemedWindow>
