<Window x:Class="KronosScreenRemote.FileManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Manager — Kronos" Width="960" Height="600" MinWidth="640" MinHeight="400"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="True"
        Background="#FF1A1A1A"
        Foreground="White">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background"      Value="#FF1A1A1A"/>
            <Setter Property="Foreground"      Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="IsReadOnly"      Value="True"/>
            <Setter Property="Padding"         Value="0"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background"  Value="#FF3A3A3A"/>
            <Setter Property="Foreground"  Value="White"/>
            <Setter Property="BorderBrush" Value="#FF666666"/>
            <Setter Property="Padding"     Value="8 3"/>
            <Setter Property="Margin"      Value="0 0 4 0"/>
        </Style>
        <Style TargetType="ListView">
            <Setter Property="Background"  Value="#FF232323"/>
            <Setter Property="Foreground"  Value="White"/>
            <Setter Property="BorderBrush" Value="#FF444444"/>
        </Style>
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background"  Value="#FF2E2E2E"/>
            <Setter Property="Foreground"  Value="#FFCCCCCC"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding"     Value="4 3"/>
        </Style>
        <Style TargetType="ListViewItem">
            <Setter Property="Foreground"  Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Margin"      Value="0 0 0 2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" Background="Transparent"
                                BorderBrush="Transparent" BorderThickness="1" Padding="2 4">
                            <GridViewRowPresenter
                                Columns="{TemplateBinding GridView.ColumnCollection}"
                                Content="{TemplateBinding Content}"
                                VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF363636"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF1A4E6E"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#FF3A8FBF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark context-menu theme -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background"            Value="#FF282828"/>
            <Setter Property="BorderBrush"           Value="#FF555555"/>
            <Setter Property="BorderThickness"       Value="1"/>
            <Setter Property="Foreground"            Value="White"/>
            <Setter Property="Padding"               Value="2"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="MenuItem">
            <Setter Property="Background"            Value="Transparent"/>
            <Setter Property="Foreground"            Value="White"/>
            <Setter Property="Padding"               Value="10 5"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF1A4E6E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FF606060"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Rectangle Height="1" Margin="4 3" Fill="#FF444444"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark drive-selector ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background"            Value="#FF3A3A3A"/>
            <Setter Property="Foreground"            Value="White"/>
            <Setter Property="BorderBrush"           Value="#FF666666"/>
            <Setter Property="BorderThickness"       Value="1"/>
            <Setter Property="MaxDropDownHeight"     Value="200"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="18"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="5 2 2 2"
                                                      VerticalAlignment="Center"/>
                                    <Path Grid.Column="1"
                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                          Fill="#FFAAAAAA" Width="8" Height="4"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Stretch="Fill" IsHitTestVisible="False"/>
                                </Grid>
                            </Border>
                            <ToggleButton Focusable="False" OverridesDefaultStyle="True"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                              RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup" Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Slide">
                                <Border MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Background="#FF2E2E2E"
                                        BorderBrush="#FF555555" BorderThickness="1">
                                    <ScrollViewer CanContentScroll="True">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF4A4A4A"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#FF88AADD"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="OverridesDefaultStyle"       Value="True"/>
            <Setter Property="Foreground"                  Value="White"/>
            <Setter Property="Padding"                     Value="6 3"/>
            <Setter Property="HorizontalContentAlignment"  Value="Stretch"/>
            <Setter Property="VerticalContentAlignment"    Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF1A4E6E"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF1A4E6E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Name cell: folders get a bright blue tint, files stay white -->
        <DataTemplate x:Key="NameCell">
            <TextBlock Text="{Binding DisplayName}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground"  Value="White"/>
                        <Setter Property="FontWeight"  Value="Normal"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                <Setter Property="Foreground" Value="#FFA8CFFF"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </DataTemplate>
    </Window.Resources>

    <!-- Shared column defs so the GridSplitter aligns nav + list + toolbar rows -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── Row 0: Navigation bars ─────────────────────────────────── -->
        <Border Grid.Row="0" Grid.Column="0" Background="#FF252525" Padding="6 4">
            <DockPanel>
                <ComboBox x:Name="LocalDriveCombo" DockPanel.Dock="Left"
                          Width="135" Height="24" Margin="0 0 4 0"
                          DropDownOpened="OnLocalDriveDropDownOpened"
                          SelectionChanged="OnLocalDriveChanged"
                          ToolTip="Select drive"/>
                <Button x:Name="BtnLocalUp" DockPanel.Dock="Left" Content="↑" Width="28" Height="24"
                        Click="OnLocalUp" ToolTip="Parent directory — drag files here to move up"
                        Margin="0 0 6 0" AllowDrop="True"
                        DragOver="OnUpDragOver" DragLeave="OnUpDragLeave" Drop="OnUpDrop"/>
                <TextBlock DockPanel.Dock="Left" Text="Local: " Foreground="#FF88DD88"
                           VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBox x:Name="LocalPathBox" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>
        <Rectangle Grid.Row="0" Grid.Column="1" Fill="#FF444444"/>
        <Border Grid.Row="0" Grid.Column="2" Background="#FF252525" Padding="6 4">
            <DockPanel>
                <Button x:Name="BtnRemoteUp" DockPanel.Dock="Left" Content="↑" Width="28" Height="24"
                        Click="OnRemoteUp" ToolTip="Parent directory — drag files here to move up"
                        Margin="0 0 6 0" AllowDrop="True"
                        DragOver="OnUpDragOver" DragLeave="OnUpDragLeave" Drop="OnUpDrop"/>
                <TextBlock DockPanel.Dock="Left" Text="Kronos: " Foreground="#FF88AADD"
                           VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBox x:Name="RemotePathBox" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <!-- ── Row 1: File lists ──────────────────────────────────────── -->
        <ListView Grid.Row="1" Grid.Column="0" x:Name="LocalList"
                  SelectionMode="Extended"
                  MouseDoubleClick="OnLocalDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name (Local)" Width="220"
                                    CellTemplate="{StaticResource NameCell}"/>
                    <GridViewColumn Header="Size" Width="70"
                                    DisplayMemberBinding="{Binding SizeText}"/>
                    <GridViewColumn Header="Modified" Width="130"
                                    DisplayMemberBinding="{Binding DateText}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <GridSplitter Grid.Row="1" Grid.Column="1" Width="4" HorizontalAlignment="Stretch"
                      Background="#FF444444"/>

        <ListView Grid.Row="1" Grid.Column="2" x:Name="RemoteList"
                  SelectionMode="Extended"
                  MouseDoubleClick="OnRemoteDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name (Kronos)" Width="220"
                                    CellTemplate="{StaticResource NameCell}"/>
                    <GridViewColumn Header="Size" Width="70"
                                    DisplayMemberBinding="{Binding SizeText}"/>
                    <GridViewColumn Header="Modified" Width="130"
                                    DisplayMemberBinding="{Binding DateText}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- ── Row 2: Per-pane toolbars ──────────────────────────────── -->
        <Border Grid.Row="2" Grid.Column="0" Background="#FF252525"
                BorderBrush="#FF444444" BorderThickness="0 1 0 0" Padding="6 4">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnUpload"         Content="→ Send to Kronos" Click="OnUpload"
                        ToolTip="Upload selected local files to Kronos"/>
                <Button x:Name="BtnLocalNewFolder" Content="New Folder"       Click="OnLocalNewFolder"/>
                <Button x:Name="BtnLocalDelete"    Content="Delete"           Click="OnLocalDelete"/>
                <Button x:Name="BtnLocalRename"    Content="Rename"           Click="OnLocalRename"/>
                <Button x:Name="BtnLocalRefresh"   Content="↺"               Click="OnLocalRefresh"
                        Width="32" Margin="0"/>
            </StackPanel>
        </Border>
        <Rectangle Grid.Row="2" Grid.Column="1" Fill="#FF444444"/>
        <Border Grid.Row="2" Grid.Column="2" Background="#FF252525"
                BorderBrush="#FF444444" BorderThickness="0 1 0 0" Padding="6 4">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnDownload"        Content="← Send to PC"   Click="OnDownload"
                        ToolTip="Download selected Kronos files to local"/>
                <Button x:Name="BtnRemoteNewFolder" Content="New Folder"      Click="OnRemoteNewFolder"/>
                <Button x:Name="BtnRemoteDelete"    Content="Delete"          Click="OnRemoteDelete"/>
                <Button x:Name="BtnRemoteRename"    Content="Rename"          Click="OnRemoteRename"/>
                <Button x:Name="BtnRemoteRefresh"   Content="↺"              Click="OnRemoteRefresh"
                        Width="32" Margin="0"/>
            </StackPanel>
        </Border>

        <!-- ── Row 3: Status bar ──────────────────────────────────────── -->
        <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3"
                Background="#FF1A1A1A" BorderBrush="#FF444444"
                BorderThickness="0 1 0 0" Padding="6 3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Grid.Column="0"
                           Foreground="#FFCCCCCC" VerticalAlignment="Center" Text="Ready"/>
                <ProgressBar x:Name="TransferProgress" Grid.Column="1"
                             Height="12" Margin="4 0 0 0"
                             Foreground="#FF88AADD" Background="#FF333333"
                             BorderBrush="#FF555555"
                             Visibility="Collapsed" Minimum="0" Maximum="100"/>
            </Grid>
        </Border>
    </Grid>
</Window>
